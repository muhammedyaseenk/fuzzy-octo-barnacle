# FastAPI and ASGI
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1
# Optional: only keep this if you also use sync psycopg anywhere
# psycopg2-binary==2.9.9

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Caching (Redis, async via redis-py)
redis==5.0.1
# aioredis removed – it is deprecated and merged into redis

# Storage & Images
minio==7.2.3
Pillow==10.2.0

# Rate Limiting
slowapi==0.1.9

# Validation & Settings
pydantic[email]==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# MFA
pyotp==2.9.0
qrcode[pil]==7.4.2

# Celery & RabbitMQ / Redis
celery[redis]==5.3.6
kombu==5.3.5
flower==2.0.1

# Development
python-dotenv==1.0.0

# Monitoring
prometheus-client==0.19.0

# HTTP Client
httpx==0.25.2

# Cloud / Integrations
boto3==1.34.34
twilio==8.12.0
firebase-admin==6.4.0

# Realtime / Utilities
python-socketio==5.11.0
psutil==5.9.6
watchdog==3.0.0

# Run pip-audit or safety or pip --use-feature=fast-deps scans to spot known CVEs